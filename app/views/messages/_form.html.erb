<%= form_for @message do |f| %>
  <%= f.text_field :subject %>
  <%= f.text_area :content %>

  <% friends_array = @friends.map { |x| [x.friend.name, x.friend_id] } %>
  <%= f.select :recipients, friends_array %>

  <%= f.submit %>
<% end %>

